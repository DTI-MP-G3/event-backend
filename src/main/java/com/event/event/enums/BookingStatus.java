package com.event.event.enums;

public enum BookingStatus {
//    'RESERVE', 'BOOKED', 'FAILED','PAYMENT_EXPIRED' 'CANCELLED', 'EXPIRED'
    RESERVED,
    BOOKED,
    FAILED,
    PAYMENT_EXPIRED,
    CANCELLED,
    EXPIRED
}
